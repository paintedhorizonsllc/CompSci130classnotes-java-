# CompSci130.classnotes.java ☕️📓

A living notebook of my **Computer Science 130** practice in **Java**: labs, class examples, and small mini-projects as I build fundamentals (and level up confidence one program at a time).

## What’s in here
- **Labs (`Lab1.java` … `Lab6.java`)**: focused exercises to build core skills.
- **Mini-project folders**:
  - `ATM/` – simple ATM-style logic and flow.
  - `ElectionResultsLab/` – election results management (arrays, totals, reporting).
- **Standalone practice files**
  - `EnergyBill.java` / `EnergyBillwithchar` – energy bill calculations (inputs + logic).
  - `Groupwork3.java` – refactored group work (includes package/cleanup updates).
- **Dated class-note folders**
  - `Nov 6/`, `Nov11DataStructures/`, `Nov13/`, `Nov18/`, `Nov20main/`, `Nov20myLibrary/`
- **Utility/library work**
  - `methodsLibrary1127` – helper methods for reuse (arrays/student management practice).

> Note: Some items may be works-in-progress or “classroom style” code (meant for learning, not production).

## How to run (quick)
### Option A: Eclipse (recommended for class)
1. Open Eclipse → **File → Open Projects from File System**
2. Select this repo folder
3. Open a file that has `public static void main(String[] args)`
4. Click **Run**

### Option B: Terminal (macOS / Windows / Linux)
1. Install a JDK (Java 17+ is great)
2. From the folder that contains the `.java` file:
   ```bash
   javac FileName.java
   java FileName
